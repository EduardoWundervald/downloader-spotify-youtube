# downloader-spotify-youtube
# python -m PyInstaller --noconsole --onefile --icon=icone.ico --add-data "icone.ico;." --collect-all spotdl --collect-all yt_dlp --collect-all pykakasi app.py

<!-- # 1. Muda para a branch
git checkout feature-login

# 2. Adiciona todos os arquivos modificados
git add .

# 3. Commita as alterações na branch 'feature-login'
git commit -m "Implementa a feature de login inicial" -->
